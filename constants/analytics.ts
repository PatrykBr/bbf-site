// PostHog event names
export enum WorkEvents {
    PHOTO_VIEW = "work_photos:image_view",
    PHOTO_SHARE = "work_photos:image_share"
}

export enum ContactEvents {
    METHOD_CLICK = "contact_methods:method_click"
}

// Share platforms
export enum SharePlatform {
    FACEBOOK = "facebook",
    WHATSAPP = "whatsapp",
    CLIPBOARD = "clipboard"
}

// View sources
export enum ViewSource {
    SHARED_LINK = "shared_link",
    GALLERY_CLICK = "gallery_click"
}
